name: CKAN master on Python 3.12 (v2)

on:
  push:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * 2,4'

jobs:
  call-reusable-workflow:
    uses: ./.github/workflows/reusable_ckan_tests.yml
    with:
      python-version: "3.12"
      ckan-version: "master"
